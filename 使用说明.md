# 电力市场研究前沿追踪 · 使用说明

这是一个保存在本机、无需服务器和账号的文献追踪网站。它会从 OpenAlex、
arXiv、Crossref 等学术数据源抓取电力市场方向论文，整理成热点排行、主题趋势、
上升关键词和可筛选的文献列表。

网站数据和历史记录都在本文件夹内，不会上传到任何地方。

---

## 一、最快的使用方式

### 只看网站

双击：

```
打开网站.bat
```

浏览器会直接打开本地的 `index.html`，显示上一次抓取到的数据。

### 更新最新文献

双击：

```
一键更新.bat
```

脚本会依次执行：

1. 到 OpenAlex、arXiv、Crossref 检索全部 18 个主题；
2. 跨来源去重，并把新文献写入本地 SQLite 数据库；
3. 重新计算热点、趋势、上升关键词；
4. 生成网页所需的 `data/data.js`、`data/papers.json`、`data/insights.json`；
5. 更新完成后自动打开 `index.html`。

一般全量更新约 2—5 分钟；日常增量更新通常 1—3 分钟。

### 更新并同步到 GitHub

双击：

```
一键更新并同步到GitHub.bat
```

它会自动完成：

1. 抓取并刷新本地网站数据；
2. `git add`、`git commit`；
3. 推送到 GitHub；
4. GitHub Pages 自动重新部署在线网站。

第一次运行时，Git 可能会弹出 GitHub 登录窗口。完成一次授权后，以后通常不需要
重复登录。

在线网站：

```
https://bahoya-lee.github.io/power-market-radar/
```

---

## 二、设置每天自动更新

### 推荐：云端自动更新

当前仓库已经配置 GitHub Actions。GitHub 会在云端按计划运行 Python 爬虫，
无需你的电脑开机：

```
每天 08:30（北京时间）自动更新
```

云端流程：

1. 恢复上一次的 SQLite 文献库缓存；
2. 抓取 OpenAlex、Crossref、arXiv；
3. 更新 `data/` 中的网站数据并提交到仓库；
4. 自动重新部署 GitHub Pages。

在线网站：

```
https://bahoya-lee.github.io/power-market-radar/
```

也可以在 GitHub 仓库的 **Actions** 页面手动点击
`Refresh data and deploy website` 立即运行一次。

### 可选：在自己的电脑上定时更新

双击：

```
注册每日更新任务.bat
```

按提示输入每天更新时间，例如：

```
08:30
```

程序会在 Windows 任务计划程序中创建名为 `PowerMarketRadar-Daily` 的任务。

注意事项：

- 电脑需要处于开机且已登录状态；
- 错过的执行时间不会自动补跑；
- 运行日志保存在 `logs/update.log`；
- 想取消自动更新，双击 `取消每日更新任务.bat`；
- 想查看任务状态，在项目目录运行：

```
python crawler\schedule.py status
```

如果已经使用 GitHub Actions 云端更新，就不需要再注册本机每日任务。
本机任务适合希望保留本地 SQLite 历史、完全不使用云端计算的用户。

---

## 三、网站怎么用

### 搜索

搜索框支持标题、摘要、作者、期刊和主题词。

输入中文也可以，例如：

- 现货
- 储能
- 碳市场
- 虚拟电厂
- 电价预测
- 需求响应

网站会自动把中文主题映射到对应的英文检索词。

按 `/` 可以直接跳到搜索框；按 `Esc` 可以关闭导出面板。

### 筛选

工具栏支持以下筛选：

- 时间范围：近 7 天、近 30 天、近 90 天、近一年、全部时间；
- 主题标签：电力现货市场、储能、碳市场、人工智能等 18 个方向；
- 数据源；
- 只看收藏；
- 只看未读；
- 只看今日新增；
- 重置筛选。

### 排序

排序方式包括：

- 最新发表；
- 相关度；
- 总被引；
- 引用速度；
- 重点期刊优先。

### 热点与趋势

右侧面板包括：

- **热点主题排行**：按产出量、增长动能、引用影响力和重点期刊占比综合计算；
- **近 12 个月主题趋势**：点击图例可以隐藏或恢复某条曲线；
- **上升关键词**：自动比较近半年与前半年，发现正在变热的新术语；
- **中文库直达检索**：可跳转知网、万方、百度学术、Google 学术；
- **来源分布**：查看各数据源贡献的文献数量。

点击热点主题即可筛选对应的文献。

### 收藏、已读与导出

每条文献支持：

- 收藏；
- 标记已读；
- 复制参考文献；
- 复制 BibTeX；
- 打开原文；
- 打开 PDF；
- 查看 DOI。

收藏和已读状态保存在浏览器本地，下次打开仍然存在。

点击右上角“导出”，可以把收藏文献导出为：

- BibTeX；
- Markdown；
- CSV；
- 参考文献格式。

---

## 四、数据保存在哪里

| 文件或目录 | 用途 |
| --- | --- |
| `data/library.db` | 本地文献库，保存全部历史记录 |
| `data/data.js` | 网站直接读取的数据 |
| `data/papers.json` | 文献列表 JSON |
| `data/insights.json` | 热点、趋势、上升关键词 |
| `data/meta.json` | 更新时间、数据源状态、错误记录 |
| `logs/update.log` | 每日自动更新日志 |

想换台电脑使用，把整个项目文件夹复制过去即可，不需要重新抓取全部历史。

如果只想更新电脑本地，使用 `一键更新.bat`；如果希望手机上的在线网站也同步
更新，使用 `一键更新并同步到GitHub.bat`。

---

## 五、当前网络限制

部分校园网、公司网络或代理环境会阻断 arXiv 的
`export.arxiv.org` 接口。遇到这种情况时：

- 程序连续失败两次后会自动跳过 arXiv；
- 其他数据源继续正常更新；
- OpenAlex 会补充预印本，尽量保留前沿内容；
- 页面顶部会显示提示，点击 ✕ 可以关闭；
- 网络恢复后，下次更新会自动重试 arXiv。

如果本机需要代理才能访问学术数据源，可以在运行前设置：

```
set PMR_PROXY=http://127.0.0.1:7890
```

也可以强制直连：

```
set PMR_PROXY_MODE=direct
```

或明确使用系统代理：

```
set PMR_PROXY_MODE=system
```

---

## 六、常见问题

### 网站没有数据

确认 `data/data.js` 存在且不是 0 字节。然后运行：

```
python crawler\run.py --offline
```

这会用本地数据库重新生成网站数据。

### 今日新增一直是 0

说明这次抓取没有发现上次之后的新文献，不一定是故障。期刊上线和预印本发布
都有周期，通常每周会有明显新增。

### 中文文献很少

OpenAlex、arXiv、Crossref 对中文期刊覆盖有限。可以在网站右侧的
“中文库直达检索”中跳到知网、万方、百度学术继续检索。

### 数据源部分失败

单个来源失败不会影响其他结果，页面会显示提示。可以关闭提示，也可以重新
双击 `一键更新.bat` 重试。

### 想重新抓取全部数据

谨慎使用以下命令，它会清空本地文献库并重新抓取：

```
python crawler\run.py --fresh
```

普通使用不需要执行。

---

## 七、常用命令

在项目目录打开 PowerShell 或 CMD：

```
python crawler\run.py
python crawler\run.py --offline
python crawler\run.py --topics spot_market,ai
python crawler\run.py --list-topics
python crawler\selftest.py
python crawler\schedule.py status
```

参数说明：

- `--offline`：不联网，只用本地库重建网站；
- `--topics`：只抓指定主题，调试时更快；
- `--list-topics`：列出所有主题 ID；
- `selftest.py`：离线自检，验证整条流水线。
